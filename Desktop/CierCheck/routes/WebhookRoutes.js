// const express = require('express');
// const {
//     stripeWebhook,
//     pandadocWebhook,
//     stripeWebhookMain,
//     veriffWebhook,
// } = require('../controller/WebhookController');

// const router = express.Router();

// router.post('/stripe', express.raw({ type: 'application/json' }), stripeWebhook);
// router.post('/stripe-main', express.raw({ type: 'application/json' }), stripeWebhookMain);
// router.post('/pandadoc', express.raw({ type: 'application/json' }), pandadocWebhook);
// router.post('/veriff', express.raw({ type: 'application/json' }), veriffWebhook);

// module.exports = router;
